# @flash-sale/web

Next.js (App Router) frontend (technical-design.md §3.1). Currently a skeleton —
one placeholder page. The real drop UI is built in S-1.1.

Scripts: `dev` (next dev), `build` (next build), `start` (next start). Default port
3000. Built by Next, not the root `tsc -b`.
