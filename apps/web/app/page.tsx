/**
 * Placeholder home page (S-E0.1c). Renders static text only — no API calls, no
 * state. The web app holds no authority: it never decides stock, price or rights
 * (NFR-9). The real drop page is built in S-1.1.
 */
export default function HomePage() {
  return (
    <main>
      <h1>Flash-Sale — web skeleton</h1>
      <p>Placeholder page. The real UI is built in S-1.1.</p>
    </main>
  );
}
