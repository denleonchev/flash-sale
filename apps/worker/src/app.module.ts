import { Module } from "@nestjs/common";

/**
 * Root module. Skeleton for S-E0.1b — empty for now. Feature modules (order
 * consumer, email, embeddings) arrive in later cards (S-E0.4 and beyond).
 */
@Module({})
export class AppModule {}
